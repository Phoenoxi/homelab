services:
  portfolio:
    image: nginx:alpine
    container_name: portfolio-site
    restart: unless-stopped
    ports:
      - "${PORTFOLIO_PORT:-8080}:80"
    volumes:
      - ${PORTFOLIO_CONTENT_PATH:-./site}:/usr/share/nginx/html:ro
